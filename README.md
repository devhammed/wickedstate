# wickedstate

A plug-n-play JavaScript UI library.

## Installation

Run the following inside your project folder (if you don't have one already, you can create using something like `mkdir my-wicked-app && cd my-wicked-app && npm init -y`)

```sh
npm install wickedstate
```

